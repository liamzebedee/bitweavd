define(['jquery', 'app/net', 'app/bencode'], function($, net, bencode){
	/*const IP = 'X';
	var node = net.connect(IP);
	net.register('ping', function(msg, args){
		// msg { 'sender', 'id', respond }
		msg.respond({ 'msg': args.msg });
	});
	var promise = node.query('ping', {msg: 'hello'});
	promise.done(function(args) {
		alert(args.msg);
	});
	*/
});
