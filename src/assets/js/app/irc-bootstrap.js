/*
 * IRC Client used for bootstrapping.
 * facilitates node discovery through a qwebirc server.
 * ~ Based on the Bitcoin bootstrap mechanism.
 */
define([], function(){
	function bootstrap() {
		var nodes = [];	
		// JOIN #bitweav
		// WHO #bitweav
		// Populate nodes
		// Return
	}

return {
	bootstrap: bootstrap
};});
